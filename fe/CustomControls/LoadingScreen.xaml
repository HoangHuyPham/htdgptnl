<UserControl x:Class="fe.CustomControls.LoadingScreen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:fe.CustomControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="450">
    <Grid Background="#80000000" Opacity="0.5">
        <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
            <!-- Spinner -->
            <Canvas Width="60" Height="60">
                <Ellipse Width="50" Height="50"
                 Stroke="White" StrokeThickness="5"
                 StrokeDashArray="2 4">
                    <Ellipse.RenderTransform>
                        <RotateTransform x:Name="SpinnerRotate" CenterX="25" CenterY="25"/>
                    </Ellipse.RenderTransform>
                    <Ellipse.Triggers>
                        <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                            <BeginStoryboard>
                                <Storyboard RepeatBehavior="Forever">
                                    <DoubleAnimation
                                Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                From="0" To="360" Duration="0:0:1"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </Ellipse.Triggers>
                </Ellipse>
            </Canvas>

            <!-- Animated Text -->
            <TextBlock x:Name="LoadingText"
               HorizontalAlignment="Center" VerticalAlignment="Bottom"
               Margin="0,70,0,0"
               FontSize="16" Foreground="White"
               Text="Loading.">

                <TextBlock.Triggers>
                    <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                        <BeginStoryboard>
                            <Storyboard RepeatBehavior="Forever">
                                <StringAnimationUsingKeyFrames Storyboard.TargetName="LoadingText"
                                                   Storyboard.TargetProperty="Text"
                                                   Duration="0:0:1.5" RepeatBehavior="Forever">
                                    <DiscreteStringKeyFrame KeyTime="0:0:0" Value="Loading." />
                                    <DiscreteStringKeyFrame KeyTime="0:0:0.5" Value="Loading.." />
                                    <DiscreteStringKeyFrame KeyTime="0:0:1" Value="Loading..." />
                                </StringAnimationUsingKeyFrames>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </TextBlock.Triggers>
            </TextBlock>
        </Grid>
    </Grid>
</UserControl>
