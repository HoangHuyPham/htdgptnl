<Window x:Class="fe.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:fe"
        mc:Ignorable="d"
        xmlns:custom_controls="clr-namespace:fe.CustomControls"
        xmlns:datacontext="clr-namespace:fe.ModelViews"
        Title="Login" Height="450" Width="800">
    <Window.DataContext>
        <datacontext:LoginWindowModelView/>
    </Window.DataContext>

    <Grid Background="Gray">
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Rectangle Grid.ColumnSpan="8" Fill="#FF2E2E2E" Height="49" />

        <TextBlock  Grid.ColumnSpan="8" Text="Login" TextAlignment="Center" FontSize="30" FontWeight="Bold" Foreground="Azure" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="20,0,0,0"></TextBlock>

        <Grid Grid.Row="2" Grid.Column="2" Grid.ColumnSpan="4" Grid.RowSpan="4">
            <StackPanel>
                <custom_controls:PlaceHolderInput TextContent="{Binding LoginDTO.Username, UpdateSourceTrigger=PropertyChanged}" PlaceHolder="username/email"/>

                <custom_controls:PlaceHolderInput Margin="0 10" TextContent="{Binding LoginDTO.Password, UpdateSourceTrigger=PropertyChanged}" PlaceHolder="password"/>

                <Button Content="Login" FontSize="24" Command="{Binding LoginCmd}" />

                <TextBlock Text="Login failed" FontSize="20" Foreground="Red" HorizontalAlignment="Center" Margin="0 10">
                    <TextBlock.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation
                                        Storyboard.TargetProperty="Opacity"
                                        From="0.0" To="1.0" Duration="0:0:1" 
                                        />
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </TextBlock.Triggers>
                </TextBlock>
            </StackPanel>
        </Grid>

    </Grid>
</Window>
